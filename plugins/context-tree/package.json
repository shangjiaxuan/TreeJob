{
  "name": "context-tree",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "generate": "node scripts/generate-contracts.mjs",
    "build": "node build.mjs",
    "shell": "npm run build && node dist/shell.mjs",
    "browse": "npm run build && node dist/browse.mjs",
    "typecheck": "npm run generate && tsc --noEmit",
    "test": "npm run build && node --test dist/test/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "package-artifact": "node scripts/package-artifact.mjs"
  },
  "dependencies": { "@modelcontextprotocol/sdk": "^1.25.3", "zod": "^4.1.0" },
  "devDependencies": { "@types/node": "^24.0.0", "esbuild": "^0.25.0", "typescript": "^5.8.0" }
}
