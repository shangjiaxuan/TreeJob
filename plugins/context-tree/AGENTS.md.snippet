# Context Tree

For non-trivial work, Context Tree is the authoritative continuation state. Inspect the active context before global memory/log searches. Create child records only for independently returnable work, keep return conditions current, and explicitly resolve compact proposals before changing the tree.
